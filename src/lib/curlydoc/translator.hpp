#pragma once

#include "interpreter.hpp"

namespace curlydoc{

// class translator : public interpreter{
// public:
// 	translator(std::string&& file_name);

// 	virtual void on_paragraph(const treeml::tree_ext& tree) = 0;

// 	virtual void on_bold(const treeml::tree_ext& tree) = 0;
// 	virtual void on_italic(const treeml::tree_ext& tree) = 0;
// 	virtual void on_underline(const treeml::tree_ext& tree) = 0;
// 	virtual void on_strikethrough(const treeml::tree_ext& tree) = 0;
// 	virtual void on_monospace(const treeml::tree_ext& tree) = 0;

// 	virtual void on_header1(const treeml::tree_ext& tree) = 0;
// 	virtual void on_header2(const treeml::tree_ext& tree) = 0;
// 	virtual void on_header3(const treeml::tree_ext& tree) = 0;
// 	virtual void on_header4(const treeml::tree_ext& tree) = 0;
// 	virtual void on_header5(const treeml::tree_ext& tree) = 0;
// 	virtual void on_header6(const treeml::tree_ext& tree) = 0;

// 	// TODO: add basic virtual functions
// };

}
